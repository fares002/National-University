# Docker Environment Variables
# Copy this file to .env and update with your actual values

# Database Configuration (Local MySQL on host machine)
# Note: DATABASE_URL is set in docker-compose.yml using host.docker.internal
# Ensure your local MySQL user has permission to accept connections from Docker containers

# JWT Configuration
JWT_SECRET_KEY=05b7847d5caa00b0f4a05fc8e4e773583211628f5e838b267efc35dee5a069dd
JWT_EXPIRES_IN=7d

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Server Configuration
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN=http://localhost
CLIENT_URL=http://localhost

# Redis Configuration (using containerized Redis)
# REDIS_HOST and REDIS_PORT are set in docker-compose.yml
REDIS_PASSWORD=

# Email Configuration
EMAIL_SERVICE=gmail
EMAIL_USER=faresosama002@gmail.com
EMAIL_PASSWORD=ccyu wfhz loiu qtxs

# Google OAuth (if using)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Frontend API URL
VITE_API_BASE_URL=http://localhost:3000/api/v1
